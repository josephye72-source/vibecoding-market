{
  "name": "vibe-coding-market",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && vite build",
    "test": "vitest",
    "test:run": "vitest --run"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "@types/node": "^25.9.3",
    "jsdom": "^29.1.1",
    "typescript": "^5.8.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
